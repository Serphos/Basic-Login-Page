document.addEventListener('DOMContentLoaded', () => {
    // Implement interactions or animations if needed
    // For OAuth, trigger the OAuth flow based on the button clicked
    // Example:
    document.querySelector('.gmail').addEventListener('click', () => {
        // Trigger OAuth flow for Gmail
        alert('Gmail OAuth flow would be initiated here.');
    });

    // Similarly, add for GitHub, Facebook, LinkedIn
});
